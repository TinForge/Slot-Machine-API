version: '3.4'

services:
  myapiproject:
    build: .
    ports:
      - "5000:80"
    depends_on:
      - mongo

  mongo:
    image: "mongo"
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: